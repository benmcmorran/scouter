{
  "name": "scouter",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "prettier . --check && tsc --noEmit && eslint --max-warnings 0 && vite build",
    "test": "vitest run",
    "preview": "vite preview",
    "format": "prettier . --write"
  },
  "devDependencies": {
    "@openstreetmap/id-tagging-schema": "^6.4.1",
    "@rushstack/eslint-config": "^3.5.0",
    "eslint": "^8.55.0",
    "jsdom": "^23.0.1",
    "prettier": "^3.1.0",
    "typescript": "^5.3.2",
    "vite": "^5.0.0",
    "vitest": "^0.34.6"
  }
}
