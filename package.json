{
  "name": "scouter",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "prettier . --check && tsc --noEmit && eslint src --max-warnings 0 && vite build",
    "preview": "vite preview",
    "format": "prettier . --write"
  },
  "devDependencies": {
    "@rushstack/eslint-config": "^3.5.0",
    "eslint": "^8.55.0",
    "prettier": "^3.1.0",
    "typescript": "^5.3.2",
    "vite": "^5.0.0"
  }
}
